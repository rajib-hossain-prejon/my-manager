import React from 'react';
import { StyleSheet } from 'react-native';
import AddServices from '../../../../main/screens/AddServices';

function index(props) {
return (
 <AddServices/>
);
}

const styles = StyleSheet.create({
container: {},
});

export default index;
