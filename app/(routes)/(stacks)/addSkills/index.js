import React from 'react';
import { StyleSheet } from 'react-native';
import AddSkills from '../../../../main/screens/AddSkills';

function index(props) {
return (
 <AddSkills/>
);
}

const styles = StyleSheet.create({
container: {},
});

export default index;
